import { InfiniteCharacterList } from "@/components/CharacterList";

const Home = () => {
    return (
        <main>
            <InfiniteCharacterList offset={0} />
        </main>
    );
};

export default Home;
